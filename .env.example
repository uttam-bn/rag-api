# Copy to .env. All optional — the API runs without any keys (extractive mode).

# Embedding backend: "tfidf" (default, no install) or "sentence-transformers"
RAG_BACKEND=tfidf

# Optional LLM for generated answers (otherwise the top chunk is returned)
# OPENAI_API_KEY=
# SARVAM_API_KEY=
